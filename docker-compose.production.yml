services:
  api:
    build:
      context: .
      dockerfile: .docker/Dockerfile
      target: api
      args:
        HTTP_PROXY: ${HTTP_PROXY:-}
        HTTPS_PROXY: ${HTTPS_PROXY:-}
        ALL_PROXY: ${ALL_PROXY:-}
        NO_PROXY: ${NO_PROXY:-}
        CODEX_VERSION: ${CODEX_VERSION:-0.145.0}
    ports:
      - "127.0.0.1:3009:3009"
    env_file: .env
    environment:
      - DATABASE_URL=/data/aif.sqlite
      - PORT=3009
      - LOG_LEVEL=info
      - PROJECTS_MOUNT=${PROJECTS_MOUNT:-/home/www}
      # Codex OAuth login proxy is dev-only. Production should rely on OPENAI_API_KEY.
      - AIF_ENABLE_CODEX_LOGIN_PROXY=false
    volumes:
      - db-data:/data
      - projects:${PROJECTS_MOUNT:-/home/www}
      - claude-auth:/home/node/.claude
      - codex-auth:/home/node/.codex
    restart: always
    read_only: true
    dns:
      - 8.8.8.8
      - 1.1.1.1
    tmpfs:
      - /tmp:noexec,nosuid,size=100m
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 1G
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3009/health'').then(r=>{if(!r.ok)throw r.status}).catch(()=>process.exit(1))"',
        ]
      interval: 30s
      timeout: 5s
      retries: 3

  web:
    build:
      context: .
      dockerfile: .docker/Dockerfile
      target: web
      args:
        HTTP_PROXY: ${HTTP_PROXY:-}
        HTTPS_PROXY: ${HTTPS_PROXY:-}
        ALL_PROXY: ${ALL_PROXY:-}
        NO_PROXY: ${NO_PROXY:-}
        CODEX_VERSION: ${CODEX_VERSION:-0.145.0}
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
    environment:
      - DOMAIN=${DOMAIN:-localhost}
    volumes:
      - ssl-certs:/etc/angie/ssl
    entrypoint:
      [
        "/bin/sh",
        "-c",
        "mkdir -p /run/angie && sed -i 's/worker_processes.*/worker_processes 2;/' /etc/angie/angie.conf && envsubst '$$DOMAIN' < /etc/angie/http.d/production.conf.template > /etc/angie/http.d/default.conf && exec angie -g 'daemon off;'",
      ]
    depends_on:
      api:
        condition: service_healthy
    restart: always
    tmpfs:
      - /tmp:noexec,nosuid,size=100m
      - /var/cache/angie:size=50m
      - /run:size=10m
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "printf 'GET /health HTTP/1.0\\r\\nHost: localhost\\r\\n\\r\\n' | nc -w 2 127.0.0.1 80 | grep -q 'HTTP/1.1 200' || exit 1",
        ]
      interval: 30s
      timeout: 5s
      retries: 3

  agent:
    build:
      context: .
      dockerfile: .docker/Dockerfile
      target: agent
      args:
        HTTP_PROXY: ${HTTP_PROXY:-}
        HTTPS_PROXY: ${HTTPS_PROXY:-}
        ALL_PROXY: ${ALL_PROXY:-}
        NO_PROXY: ${NO_PROXY:-}
        CODEX_VERSION: ${CODEX_VERSION:-0.145.0}
    env_file: .env
    environment:
      - DATABASE_URL=/data/aif.sqlite
      - API_BASE_URL=http://api:3009
      - AGENT_BYPASS_PERMISSIONS=true
      - LOG_LEVEL=info
      - PROJECTS_MOUNT=${PROJECTS_MOUNT:-/home/www}
      # Codex OAuth login proxy is dev-only. Production should rely on OPENAI_API_KEY.
      - AIF_ENABLE_CODEX_LOGIN_PROXY=false
    volumes:
      - db-data:/data
      - projects:${PROJECTS_MOUNT:-/home/www}
      - claude-auth:/home/node/.claude
      - codex-auth:/home/node/.codex
    depends_on:
      api:
        condition: service_healthy
    restart: always
    dns:
      - 8.8.8.8
      - 1.1.1.1
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 1G
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"

  mcp:
    build:
      context: .
      dockerfile: .docker/Dockerfile
      target: mcp
      args:
        HTTP_PROXY: ${HTTP_PROXY:-}
        HTTPS_PROXY: ${HTTPS_PROXY:-}
        ALL_PROXY: ${ALL_PROXY:-}
        NO_PROXY: ${NO_PROXY:-}
        CODEX_VERSION: ${CODEX_VERSION:-0.145.0}
    ports:
      - "127.0.0.1:3100:3100"
    env_file: .env
    environment:
      - DATABASE_URL=/data/aif.sqlite
      - MCP_TRANSPORT=http
      - MCP_PORT=3100
      - LOG_LEVEL=info
      - PROJECTS_MOUNT=${PROJECTS_MOUNT:-/home/www}
    volumes:
      - db-data:/data
      - projects:${PROJECTS_MOUNT:-/home/www}
    depends_on:
      api:
        condition: service_healthy
    restart: always
    read_only: true
    tmpfs:
      - /tmp:noexec,nosuid,size=100m
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'node -e "fetch(''http://localhost:3100/health'').then(r=>{if(!r.ok)throw r.status}).catch(()=>process.exit(1))"',
        ]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  db-data:
  projects:
  claude-auth:
  codex-auth:
  ssl-certs:

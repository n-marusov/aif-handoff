/**
 * Отдельный подпуть пакета (`@aif/shared/server`): доступ к базе для серверного кода.
 *
 * Вынесен из основного входа намеренно: index.ts импортируется и фронтендом, а драйвер
 * SQLite в браузерную сборку попадать не должен. Это третья точка входа рядом с `.` и
 * `./browser`.
 */
export { getDb, createTestDb, closeDb } from "./db.js";

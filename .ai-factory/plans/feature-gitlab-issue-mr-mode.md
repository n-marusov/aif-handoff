# Implementation Plan: GitLab Issue-to-MR Mode

Branch: feature/gitlab-issue-mr-mode
Created: 2026-08-13
Updated: 2026-08-13 (TDD restructure — tests first, example references, missing tests added)

## Original Request

Add GitLab as a second repository-hosting provider by mirroring the existing GitHub Issue-to-PR integration (Option A) 1:1 — issues sync to tasks, merge requests get published, mirroring the GitHub mode without refactoring any GitHub code.

## Settings

- Testing: yes (TDD — failing tests are written before each implementation task)
- Logging: verbose
- Docs: yes

## Roadmap Linkage

Milestone: "none"
Rationale: The current roadmap has no GitLab integration milestone (GitHub plan used "none" for the same reason).

## Research Context

Source: .ai-factory/RESEARCH.md (Active Summary)

Goal: Add GitLab as a second repository-hosting provider so issues sync to tasks and merge requests get published, mirroring the existing GitHub mode 1:1.

Constraints:
- No new packages; work stays inside existing workspaces (shared, data, api, agent, web).
- DB access only via @aif/data (DB boundary rule).
- Append-only migrations: next free version is v29 (v28 is the last landed).
- Structured error classification: GitLabApiError must carry httpStatus + adapterCode + retryAt; never branch on message text.
- Nullable cast rule: parsers (eligibility/snapshot) must keep explicit `| null` unions and guard before access.
- Runtime Adapter Sync Rule / docs/providers.md: NOT affected (GitLab is not an AI runtime adapter).
- Pencil sync required only if a new visual UI component is introduced; prefer reusing existing primitives.
- UI theme rules; no expensive CSS properties.
- Every package >=70% coverage (vitest v8 thresholds in each package's vitest.config.ts); finish with `npm run ai:validate`.

Decisions (user-confirmed):
- Option A (mirror): add gitlab_* tables/routes/services alongside github_*; do not refactor GitHub code.
- GIT_PROVIDER env (`z.enum(["github","gitlab"]).default("github")`) — deployment-level provider selector. A mode is active ONLY when GIT_PROVIDER matches the provider AND its rollout flag is true. Backward compatible: default "github" + existing AIF_GITHUB_ISSUE_PR_ENABLED = current behavior, zero migration.
- AIF_GITLAB_ISSUE_MR_ENABLED (boolean, default false) — GitLab rollout gate, surfaced as gitlabIssueMrEnabled in settings overview. GitHub flag stays as-is.
- AIF_GITLAB_BASE_URL global env (default `https://gitlab.com/api/v4`) — self-hosted support WITHOUT per-connection baseUrl field (no schema/UI churn). Non-secret.
- Default token env var GITLAB_TOKEN; connect schema validates `namespace/project` + `GITLAB_*` prefix.
- Reuse git push to origin for branch publication; connection stays one-per-project (projectId PK). GIT_PROVIDER is global, so only one provider is active per deployment → no remote ambiguity.
- prChecksStatus source: commit statuses endpoint (NOT pipelines) — CONFIRMED, see mapping in research.
- reviewState source (CONFIRMED Option A): MR approvals endpoint (available on Free tier) → "approved" ⇔ approvals.approved=true, else "pending". NO "changes_requested"/auto-rework in v1; task resumes only via human action. Unresolved-discussions → changes_requested deferred as a v1.1 spike.
- GitLab MR note marker CONFIRMED: "<!-- aif-gitlab-review -->" (analog of GitHub REVIEW_MARKER).

Open questions: ALL RESOLVED (2026-08-13).

## Test Strategy (TDD)

Every phase follows **red → green → refactor**: a failing-test task precedes its implementation task. Before writing an implementation task, run its test task and confirm the new tests fail (red); implement until green; then run the phase's package test + coverage suites.

### Example test references (mirror these files 1:1)

| New GitLab test file | GitHub counterpart to mirror | Key cases in the counterpart |
|---|---|---|
| `packages/data/src/__tests__/gitlab.test.ts` | `packages/data/src/__tests__/github.test.ts` | `upserts and deletes one connection per project without persisting a token`; `is idempotent and refreshes the same task`; `updates pull request linkage on the existing issue`; `creates a linked task in done when an open pull request already exists`; `pauses a task when its source issue disappears`. Setup pattern: `createTestDb()` from `@aif/shared/server`, `vi.mock("@aif/shared/server")` returning `getDb: () => testDb.current`, insert `projects` row in `beforeEach`. |
| `packages/shared/src/__tests__/schema.test.ts` (NEW — shared has zero tests today) | `packages/api/src/__tests__/settings.test.ts` (env-derived settings assertions) + drizzle table usage in `packages/data/src/__tests__/github.test.ts` | Assert `gitlabRepositories`/`gitlabIssues` table columns via `getDb().select()` on a `createTestDb()`; assert migration v29 exists in `MIGRATIONS` with append-only version; assert `gitlab_issues` has `global_id` + `iid` but NO `node_id` and NO `baseUrl`. |
| `packages/shared/src/__tests__/env.test.ts` (NEW) | `packages/api/src/__tests__/settings.test.ts` (`buildSettingsOverview` env assertions) | `GIT_PROVIDER` defaults to `"github"`, accepts `"gitlab"`, rejects unknown values; `AIF_GITLAB_ISSUE_MR_ENABLED` defaults `false`; `AIF_GITLAB_BASE_URL` defaults `https://gitlab.com/api/v4`; combined with `AIF_GITHUB_ISSUE_PR_ENABLED` both providers can be configured but only `GIT_PROVIDER`-matching mode activates. |
| `packages/api/src/__tests__/gitlab.test.ts` | `packages/api/src/__tests__/github.test.ts` | `classifies rate limits from structured HTTP fields` (429 + Retry-After → `rate_limited`); `applies label, assignee, and milestone eligibility`; `rejects GitLab routes while the rollout flag is disabled`; `connects a repository and performs an idempotent empty sync`; `rejects a connection when its credential environment variable is absent`; `imports an issue with a closing MR when approvals are pending/approved` (it.each); `skips closed orphaned issues and links MRs to existing tasks`; `creates one MR and reuses it on repeated publication`. Setup pattern: `new Hono()` + `app.route("/projects", gitlabRouter)`, `vi.stubGlobal("fetch", ...)` with `jsonResponse(...)` chains, `vi.stubEnv("GITLAB_TEST_TOKEN", ...)` + `resetEnvCache()`. |
| `packages/api/src/__tests__/settings.test.ts` (extend) | existing `settings overview — GitHub issue-to-PR flag` describe | Add `describe("settings overview — provider selector")`: asserts `gitProvider` follows `GIT_PROVIDER` and `gitlabIssueMrEnabled` follows `AIF_GITLAB_ISSUE_MR_ENABLED`. |
| `packages/agent/src/__tests__/gitlabWorkflow.test.ts` | `packages/agent/src/__tests__/githubWorkflow.test.ts` | `syncs due repositories once per interval` (60s interval map, `internalApiHeaders` POST to `/projects/:id/gitlab/sync`); `does not inspect repositories or call GitLab paths while the rollout flag is disabled`. Setup pattern: `vi.mock("@aif/data")`, `vi.mock("@aif/shared")` with `getEnv: () => validateEnv(process.env)`, `vi.stubEnv("API_BASE_URL", ...)`. |
| `packages/agent/src/__tests__/coordinator.test.ts` (extend) | existing coordinator test (mocks workflows) | Assert `synchronizeGitLabProjects` is invoked in `runPollCycle` and `publishGitLabTask` in implementer/reviewer stages only when `GIT_PROVIDER === "gitlab"` (mirror how `publishGitHubTask`/`synchronizeGitHubProjects` are asserted). |
| `packages/web/src/__tests__/ProjectSelector.test.tsx` (extend) | existing GitHub block coverage in the same file | Mock `useProjectGitLab`/`useConnectProjectGitLab`/`useDisconnectProjectGitLab`/`useSyncProjectGitLab` + `useSettings` returning `{ gitProvider, gitlabIssueMrEnabled }`; add `hides GitLab project controls while gitlabIssueMrEnabled is false`; `shows GitLab block only when gitProvider === "gitlab"`; `hides GitHub block when gitProvider === "gitlab"`; connect/sync/disconnect flow with toast. |
| `packages/web/src/__tests__/TaskDetailHeader.test.tsx` (extend) | `leaves the final decision on a GitHub pull request` in the same file | Add `leaves the final decision on a GitLab merge request`: render task with `gitlab` link (`iid: 154`, `webUrl`, `mrIid`, `mrUrl`, `mrState: "open"`), expect `GITLAB #154` badge and no `Approve`/`Request changes` buttons. |
| `packages/web/src/__tests__/TaskDescription.test.tsx` (verify) | `keeps synchronized descriptions read-only` in the same file | The `readOnly` prop test is already generic; add nothing unless `TaskDetail` passes `readOnly` for `task.gitlab` — cover that wiring in `TaskDetail.test.tsx` if needed. |

### Missing tests this plan adds beyond the GitHub mirror

1. **Shared package tests (currently zero)** — `packages/shared/src/__tests__/schema.test.ts` + `env.test.ts`. Shared's vitest coverage includes `schema.ts` and `env.ts` at a 70% threshold but no tests exist; the GitHub feature never added them. These are required for `npm run ai:validate` to stay green once `gitlabRepositories`/`gitlabIssues`/new env vars land.
2. **GIT_PROVIDER cross-provider gating** — `packages/api/src/__tests__/gitlab.test.ts` (GitLab routes → 403 `feature_disabled` when `GIT_PROVIDER=github`) AND a new test in `packages/api/src/__tests__/github.test.ts` (GitHub routes → 403 when `GIT_PROVIDER=gitlab`) to lock the "only one provider active per deployment" contract.
3. **GitLab approvals-only reviewState** — API test asserting `reviewState` is `"approved"` ⇔ `approvals.approved === true` and `"pending"` otherwise, and that there is NO `changes_requested`/auto-rework transition (v1 contract).
4. **Commit statuses fold with `allow_failure`** — API test mirroring the GitHub check-run fold: `failed && !allow_failure` → `failure`; `pending`/`running` → `pending`; `failed && allow_failure` → non-blocking (fold like success); `canceled`/`skipped` → fold like success; empty → `null`.
5. **Agent publish path** — `publishGitLabTask` tests: branch push (`execFileSync git push --set-upstream origin`), internal API publish POST payload (branch/commitSha/implementationLog/reviewComments), `StageManualBlockError` on push/API failure, activity log line `[gitlab] Published <branch> for issue #<iid>`, and skip when `GIT_PROVIDER !== "gitlab"`.
6. **Agent workflow GIT_PROVIDER gate** — `gitlabWorkflow.test.ts` asserts both `GIT_PROVIDER=github` AND `AIF_GITLAB_ISSUE_MR_ENABLED=false` skip sync; `githubWorkflow.test.ts` gains a `GIT_PROVIDER=gitlab` skip case.
7. **Coordinator wiring** — extend `coordinator.test.ts`: `synchronizeGitLabProjects` in `runPollCycle` and `publishGitLabTask` in implementer/reviewer stages.
8. **Web settings surface** — `useSettings.test.tsx` + `ProjectSelector.test.tsx` cover `gitProvider`/`gitlabIssueMrEnabled` in the settings response and UI gates.

## Commit Plan

- **Commit 1** (after tasks 1-2): "feat: add GitLab repository and issue synchronization model"
- **Commit 2** (after tasks 3-4): "feat: add GitLab REST orchestration"
- **Commit 3** (after tasks 5-6): "feat: add GitLab agent workflow"
- **Commit 4** (after tasks 7-9): "feat: complete GitLab UI, docs, and validation"

## Tasks

### Phase 1: Persistent GitLab source-of-truth model (TDD)

- [x] Task 1 (TESTS): Write failing shared + data-layer tests for the GitLab source-of-truth model, BEFORE any schema/data implementation exists. New files: `packages/shared/src/__tests__/schema.test.ts`, `packages/shared/src/__tests__/env.test.ts` (shared currently has zero tests — these also fix the shared coverage gap), `packages/data/src/__tests__/gitlab.test.ts`. Mirror `packages/data/src/__tests__/github.test.ts` (createTestDb + `vi.mock("@aif/shared/server")` + insert `projects` row) for: upsert/delete one connection per project without persisting a token; idempotent `importGitLabIssueTask` refreshing the same task; `updateGitLabMergeRequest` MR linkage + reviewState (`approved`/`pending`) + fingerprint; linked task created in `done` when an open MR already exists; `markGitLabIssueUnavailable` pauses the task; `getGitLabIssueReviewFingerprint` round-trip. Add schema tests asserting `gitlab_issues` columns include `global_id` + `iid` but exclude `node_id` and `baseUrl`; assert migration v29 is appended after v28 and its `version` is `29`; assert env defaults (`GIT_PROVIDER` → `"github"`, `AIF_GITLAB_ISSUE_MR_ENABLED` → `false`, `AIF_GITLAB_BASE_URL` → `https://gitlab.com/api/v4`). Log test fixtures at DEBUG where relevant. Red gate: `npm run test --workspace=@aif/shared` and `npm run test --workspace=@aif/data` must FAIL on missing `gitlab*` exports. Files: `packages/shared/src/__tests__/schema.test.ts`, `packages/shared/src/__tests__/env.test.ts`, `packages/data/src/__tests__/gitlab.test.ts`.
- [x] Task 2 (IMPL): Make Task 1 green. Add append-only migration v29, Drizzle schema, shared browser-safe contracts, env surface, and the `@aif/data` layer. Migration v29 creates `gitlab_repositories` (project_id PK → projects ON DELETE CASCADE, namespace, name, web_url, default_branch, token_env_var DEFAULT 'GITLAB_TOKEN', eligibility_json DEFAULT '{}', enabled, last_synced_at, sync_error, created_at, updated_at) and `gitlab_issues` (project_id + iid PK, global_id NOT NULL, task_id UNIQUE → tasks ON DELETE SET NULL, web_url, state CHECK open/closed, metadata_json, source_updated_at, last_synced_at, sync_error, mr_iid, mr_url, mr_state CHECK open/closed/merged, mr_checks_status CHECK pending/success/failure, review_state CHECK pending/approved, review_fingerprint, created_at, updated_at). NO node_id column (global id replaces it), NO baseUrl column (env-driven), NO last_review_id (approvals-only v1 — no change-detection-driven rework). Add `gitlabRepositories`/`gitlabIssues` tables + row types to `packages/shared/src/schema.ts`, `GitLabEligibility` (same shape as GitHub), `GitLabRepositoryConnection`, `GitLabIssueCommentSnapshot`, `GitLabIssueSnapshot`, `GitLabIssueLink`, and `Task.gitlab?: GitLabIssueLink | null` to `packages/shared/src/types.ts`, export from node `index.ts` + browser `browser.ts`. Add env vars to `packages/shared/src/env.ts`: `GIT_PROVIDER: z.enum(["github","gitlab"]).default("github")`, `AIF_GITLAB_ISSUE_MR_ENABLED: booleanEnvSchema.default(false)`, `AIF_GITLAB_BASE_URL` string with default `https://gitlab.com/api/v4`. Mirror `packages/data/src/github.ts` in `packages/data/src/gitlab.ts` with iid-based linkage (namespace/project replaces owner/repo): `findGitLabRepository`, `listEnabledGitLabRepositories`, `upsertGitLabRepository`, `deleteGitLabRepository`, `recordGitLabRepositorySync`, `importGitLabIssueTask` (plan path `gitlab-issue-<iid>`, tags `["gitlab", ...labels]`, executor history actor `gitlab-sync`/"GitLab Sync", audit `gitlab.issue_imported`, initial status `done` when an MR is linked), `findGitLabIssue`, `findGitLabIssueByTaskId`, `listGitLabIssues`, `markGitLabIssueUnavailable`, `updateGitLabMergeRequest` (mrIid/mrUrl/mrState/mrChecksStatus/reviewState/reviewFingerprint), `getGitLabIssueReviewFingerprint`. Export everything from `packages/data/src/index.ts`. Parsers (eligibility/snapshot) must keep explicit `| null` unions and guard before access (Nullable Cast Rule). Log repository sync mutations at DEBUG/INFO and structured failures at ERROR without secrets. Green gate: `npm run test` + `npm run coverage --workspace=@aif/shared` and `--workspace=@aif/data`. Files: `packages/shared/src/schema.ts`, `packages/shared/src/db.ts`, `packages/shared/src/types.ts`, `packages/shared/src/env.ts`, `packages/shared/src/index.ts`, `packages/shared/src/browser.ts`, `packages/data/src/gitlab.ts`, `packages/data/src/index.ts`. (depends on 1)

### Phase 2: GitLab REST orchestration (TDD)

- [x] Task 3 (TESTS): Write failing API tests BEFORE the GitLab service/routes exist. New file `packages/api/src/__tests__/gitlab.test.ts` mirroring `packages/api/src/__tests__/github.test.ts` (Hono app + `jsonResponse` fetch chains + `vi.stubEnv` + `resetEnvCache`): rate-limit classification from 429 + Retry-After → `rate_limited`; eligibility filter; commit statuses fold (missing case set: failed + allow_failure → non-blocking, pending/running → pending, canceled/skipped → success-like, empty → null); rejects GitLab routes while the rollout flag is disabled; rejects GitLab routes when `GIT_PROVIDER=github`; connects a repository (validates `namespace/project` via `GET /projects/:url_encoded_path`) and performs an idempotent empty sync; rejects a connection when its `GITLAB_*` env var is absent; imports an issue with a closing MR when approvals are `pending`/`approved` (it.each — assert reviewState and NO status change to `implementing`); skips closed orphaned issues and links MRs to existing tasks; creates one MR and reuses it on repeated publication (assert exactly one POST `/merge_requests` and one marker-note POST). Extend `packages/api/src/__tests__/github.test.ts` with a cross-provider case: GitHub routes → 403 when `GIT_PROVIDER=gitlab`. Extend `packages/api/src/__tests__/settings.test.ts` with `describe("settings overview — provider selector")` asserting `gitProvider` and `gitlabIssueMrEnabled`. Red gate: `npm run test --workspace=@aif/api` must FAIL (missing `gitlabRouter`/`GitLabClient`/settings fields). Files: `packages/api/src/__tests__/gitlab.test.ts`, `packages/api/src/__tests__/github.test.ts`, `packages/api/src/__tests__/settings.test.ts`.
- [x] Task 4 (IMPL): Make Task 3 green. Implement native-fetch GitLab REST v4 client and validated project endpoints. `packages/api/src/services/gitlab.ts`: `GitLabApiError` carrying `httpStatus` + `adapterCode` (`authentication`/`forbidden`/`not_found`/`rate_limited`/`validation`/`upstream`) + `retryAt`; `GitLabClient` with base URL from `AIF_GITLAB_BASE_URL`, `PRIVATE-TOKEN` header, paginated `list`, `getRepository` (`/projects/:url_encoded_path` — validate on connect), `listIssues` (`?scope=all&state=opened&order_by=updated_at`), `listIssueNotes`, `listMergeRequests`/`getMergeRequest` (source_branch filter), `createMergeRequest`/`updateMergeRequest`, `getMergeRequestApprovals` (`approved`, `approved_by`), `getCommitStatuses` (`/repository/commits/:sha/statuses`; fold pending/running/success/failed/canceled/skipped with `allow_failure` like the GitHub combined fold), `upsertMarkerNote` (marker `<!-- aif-gitlab-review -->`); 429 + Retry-After → `rate_limited`. Helpers: `issueIsEligible` (labels/assignee/milestone map 1:1; author via `username`), `findMergeRequestClosingIssue` (reuse "Closes #N" regex), `toIssueSnapshot` (iid + global id + web_url), `latestReviewState` → approvals-only: `approved` ⇔ `approvals.approved === true`, else `pending` (NO `changes_requested` in v1), `reviewFingerprint`. `packages/api/src/routes/gitlab.ts` (mounted as `app.route("/projects", gitlabRouter)` in `packages/api/src/index.ts`): middleware gate `GIT_PROVIDER !== "gitlab" || !AIF_GITLAB_ISSUE_MR_ENABLED` → 403 `feature_disabled`; `tokenFor` enforces `GITLAB_*` prefix; GET/PUT/DELETE `/:id/gitlab`, POST `/:id/gitlab/sync` (import eligible issues, discover closing MRs, closed issue → pause, MR merged → verified, MR closed unmerged → pause, approvals reflected as reviewState — no auto-rework), POST `/:id/gitlab/tasks/:taskId/publish` (create/update MR with `Closes #<iid>` body, upsert review note by fingerprint, never merges). Add `GIT_PROVIDER === "github"` gate to the existing `routes/github.ts` middleware (one-line addition — a mode is active only when GIT_PROVIDER matches; default "github" preserves current behavior). `packages/api/src/schemas.ts`: `gitlabConnectSchema` (repository `namespace/project` regex, tokenEnvVar `GITLAB_*` regex default `GITLAB_TOKEN`, enabled, eligibility), `gitlabSyncSchema {}`, `gitlabPublishSchema` (branch/commitSha/implementationLog/reviewComments). `packages/api/src/routes/settings.ts`: expose `gitProvider` + `gitlabIssueMrEnabled` in settings overview. Handle 401/403, rate limits, closed issues/MRs using structured HTTP fields; log external calls without authorization headers. Green gate: `npm run test` + `npm run coverage --workspace=@aif/api`. Files: `packages/api/src/services/gitlab.ts`, `packages/api/src/routes/gitlab.ts`, `packages/api/src/routes/github.ts`, `packages/api/src/routes/settings.ts`, `packages/api/src/schemas.ts`, `packages/api/src/index.ts`. (depends on 2, 3)

### Phase 3: Autonomous terminal workflow (TDD)

- [x] Task 5 (TESTS): Write failing agent tests BEFORE `gitlabWorkflow.ts` exists. New file `packages/agent/src/__tests__/gitlabWorkflow.test.ts` mirroring `packages/agent/src/__tests__/githubWorkflow.test.ts` (`vi.mock("@aif/data")`, `vi.mock("@aif/shared")` with `getEnv: () => validateEnv(process.env)`, `vi.stubEnv("API_BASE_URL", ...)`): syncs due repositories once per 60s interval with POST to `/projects/:id/gitlab/sync`; skips sync when `GIT_PROVIDER=github`; skips sync when `AIF_GITLAB_ISSUE_MR_ENABLED=false`; `publishGitLabTask` skips when `GIT_PROVIDER !== "gitlab"`; `publishGitLabTask` pushes the branch (`git push --set-upstream origin <branch>`), POSTs the publish payload (branch/commitSha/implementationLog/reviewComments) to `/projects/:id/gitlab/tasks/:taskId/publish`, and appends the `[gitlab] Published <branch> for issue #<iid>` activity log; `publishGitLabTask` throws `StageManualBlockError` on push failure and on non-OK API response with `retryAt` messaging. Extend `packages/agent/src/__tests__/githubWorkflow.test.ts` with a `GIT_PROVIDER=gitlab` skip case (GitHub workflow must not run when the provider is GitLab). Extend `packages/agent/src/__tests__/coordinator.test.ts` to assert `synchronizeGitLabProjects` is called in `runPollCycle` and `publishGitLabTask` in implementer/reviewer stages when `GIT_PROVIDER=gitlab`, and that GitHub equivalents are skipped. Red gate: `npm run test --workspace=@aif/agent` must FAIL. Files: `packages/agent/src/__tests__/gitlabWorkflow.test.ts`, `packages/agent/src/__tests__/githubWorkflow.test.ts`, `packages/agent/src/__tests__/coordinator.test.ts`.
- [x] Task 6 (IMPL): Make Task 5 green. Reuse the existing task branch/worktree and commit gate, push the persisted issue branch, request MR publication through the internal API, publish automated review output once per revision, and use Done as the terminal MR-ready state without merging. `packages/agent/src/gitlabWorkflow.ts` mirrors `githubWorkflow.ts`: `synchronizeGitLabProjects` (skip when `GIT_PROVIDER !== "gitlab" || !AIF_GITLAB_ISSUE_MR_ENABLED`; POST `/projects/:id/gitlab/sync` via `internalApiHeaders()`, 60s interval map) and `publishGitLabTask` (skip gates; `ensureAutoQueueTaskCommit`; `git push --set-upstream origin <branch>` to execution root; POST `/projects/:id/gitlab/tasks/:taskId/publish` with branch/commitSha/implementationLog/reviewComments; `StageManualBlockError` on push/API failure with retryAt rate-limit messaging; activity log `[gitlab] Published <branch> for issue #<iid>`). Poll configured repositories for issue/MR changes; only a merged MR advances to Verified. Add the same `GIT_PROVIDER === "github"` skip gates to `githubWorkflow.ts` (mode active only when GIT_PROVIDER matches). Wire in `packages/agent/src/coordinator.ts`: call `synchronizeGitLabProjects()` in `runPollCycle` (after `processDueScheduledTasks`), call `publishGitLabTask(task.id, project.rootPath)` in the implementer and reviewer stages (mirroring `publishGitHubTask`). Log branch, MR, and handoff state transitions without credentials. Green gate: `npm run test` + `npm run coverage --workspace=@aif/agent`. Files: `packages/agent/src/gitlabWorkflow.ts`, `packages/agent/src/githubWorkflow.ts`, `packages/agent/src/coordinator.ts`. (depends on 4, 5)

### Phase 4: UI, documentation, and validation (TDD)

- [x] Task 7 (TESTS): Write failing web tests BEFORE the GitLab UI exists. Extend `packages/web/src/__tests__/ProjectSelector.test.tsx` (mirror the existing GitHub mocks in the same file): add `useProjectGitLab`/`useConnectProjectGitLab`/`useDisconnectProjectGitLab`/`useSyncProjectGitLab` mocks and `useSettings` returning `{ gitProvider, gitlabIssueMrEnabled }`; add `hides GitLab project controls while gitlabIssueMrEnabled is disabled`; `shows the GitLab block only when gitProvider === "gitlab"`; `hides the GitHub block when gitProvider === "gitlab"`; `connects a GitLab repository and shows the Connected badge`; `disconnects and syncs with toast feedback`. Extend `packages/web/src/__tests__/TaskDetailHeader.test.tsx` with `leaves the final decision on a GitLab merge request` (task with `gitlab` link, expect `GITLAB #154` badge, no `Approve`/`Request changes` buttons) — mirror the GitHub case. Verify `packages/web/src/__tests__/TaskDescription.test.tsx` still covers the generic `readOnly` path and cover `TaskDetail` passing `readOnly` for `task.gitlab` in `TaskDetail.test.tsx` if not already generic. Extend `packages/web/src/__tests__/useSettings.test.tsx` for `gitProvider`/`gitlabIssueMrEnabled` in the settings response. Red gate: `npm run test --workspace=@aif/web` must FAIL. Files: `packages/web/src/__tests__/ProjectSelector.test.tsx`, `packages/web/src/__tests__/TaskDetailHeader.test.tsx`, `packages/web/src/__tests__/TaskDetail.test.tsx`, `packages/web/src/__tests__/useSettings.test.tsx`.
- [x] Task 8 (IMPL): Make Task 7 green. Extend existing project edit/settings and task detail components with GitLab repository connection, eligibility filters, manual sync, connection status, and issue/MR links, gated behind `gitProvider === "gitlab" && gitlabIssueMrEnabled`. `packages/web/src/lib/api.ts`: `GitLabProjectState { connection, issues }`, `getProjectGitLab`/`connectProjectGitLab`/`disconnectProjectGitLab`/`syncProjectGitLab`, `SettingsResponse.gitProvider` + `gitlabIssueMrEnabled`. `packages/web/src/hooks/useProjects.ts`: `useProjectGitLab`, `useConnectProjectGitLab`, `useDisconnectProjectGitLab`, `useSyncProjectGitLab` (mirror GitHub hooks + invalidation). `packages/web/src/components/project/ProjectSelector.tsx`: parallel GitLab block in the edit dialog (guarded by `gitProvider === "gitlab" && gitlabIssueMrEnabled`) with `namespace/project` input, `GITLAB_TOKEN` env var input, labels/assignee/milestone eligibility inputs, Connected/Token missing badge, connect/disconnect/sync actions, syncError display — reuse existing `Input`/`Badge`/`Button` primitives, add no new visual component (no Pencil sync needed). `packages/web/src/components/task/TaskDetailHeader.tsx`: render `GITLAB #<iid>` badge when `task.gitlab` exists; gate local approve/request-changes actions on `task.gitlab` (mirror GitHub `!task.github` filter). `packages/web/src/components/task/TaskDetail.tsx` + `TaskDescription.tsx`: wire the existing generic `readOnly` prop for synced tasks (`task.gitlab`). Log client mutation outcomes through existing diagnostics. Green gate: `npm run test` + `npm run coverage --workspace=@aif/web`, then full `npm run ai:validate`. Files: existing files under `packages/web/src/components/project/`, `packages/web/src/components/task/`, `packages/web/src/hooks/`, `packages/web/src/lib/api.ts`. (depends on 4, 7)
- [x] Task 9: Document GitLab authentication permissions, configuration, API contracts, workflow behavior, recovery semantics, and the explicit no-auto-merge guarantee; run every touched package checklist, coverage, and `npm run ai:validate`. Add a "GitLab Issue-to-MR" section to `docs/api.md` (mirror the GitHub section: feature_disabled 403 semantics, token env vars never in bodies, GET state / PUT-DELETE connect / POST sync / POST publish contracts, approvals-only reviewState, merged MR → verified), update `docs/architecture.md` (workflow description + Database list entries for `gitlab_repositories`/`gitlab_issues`), update `docs/configuration.md` (GIT_PROVIDER selector + mode-activation semantics, AIF_GITLAB_ISSUE_MR_ENABLED, AIF_GITLAB_BASE_URL, GITLAB_TOKEN). `docs/providers.md` is NOT affected (GitLab is not an AI runtime adapter). `README.md` optional. Run `npm run ai:validate`; every touched package >=70% coverage; complete CHECKLIST.md items for touched packages (shared/data/api/agent/web). Files: `docs/api.md`, `docs/architecture.md`, `docs/configuration.md`, `README.md` (optional), and package checklists as applicable. (depends on 6, 8)
<!-- Commit checkpoint: tasks 7-9 -->
